version: '3'

services:
  keycloak:
    image: quay.io/keycloak/keycloak:legacy
    container_name: keycloak-server
    environment:
      KEYCLOAK_USER: admin
      KEYCLOAK_PASSWORD: Pa55w0rd
    ports:
      - 8180:8080
